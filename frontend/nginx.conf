server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    # Optional proxy for API calls
    location /api {
        proxy_pass http://backend:8000;
    }
}
